# 修订 6 · 到达层、候机廊核对（2026-09-22）

当前模型：23,403,104 字节，1167 个网格，306710 个三角面；SHA-256 `66d5fbca9e63b2cfddfba3b2466edde914a0e5e5ea8529dbfeb8ae1cd9ba4b04`。

新增 [到达层与候机廊叠图](arrival-finger-compare.html)，直接使用本轮修改前后 GLB 的投影，分别以 P02 和 P01 为底图。原出发大厅叠图仍保留。

- 到达层：五个编号转盘位置依 P02 配准修正，旧版 46 m 的均匀间距改为约 20–23 m 的图示相对间距；转盘长度从 33 m 改为 22 m。具体宽度、端部曲线与后场输送带仍未测定。
- 候机廊：核对登机口两侧编号与顺序；新增偏西侧四组商业/服务设施，座椅改为东侧为主、西侧组团间隙分布。各登机口横向通路保留。
- 实际网格通行检查：两侧安检各自可达、同时封闭禁止绕行；到达夹层连续、行李厅至出口可达；24 条路线均无阻挡。缓坡和廊桥支撑检查通过。
- 未完成项明确保留：到达层两端服务体量、后场设备、未编号 U 形设施；候机廊商铺精确外形/功能细分、真实座椅数量、登机口的实测纵向间距。到达夹层参考实拍，不能从 1F 图强行推定完整夹层平面。

以下是上版大厅修订报告；其中的模型哈希及统计仅属历史，本页顶部和 tests/delivery-manifest.json 为当前版本。

---

# 高崎 T4 修订 5 · 出发大厅地图配准纠错

日期：2026-09-22。模型：23,192,312 字节，1,145 个命名网格，303,990 个三角面。

SHA-256：`ff8744f02bfceaadb2aeee052dbf8bc591dd5fc9067349ce7b2875a4ef9797e0`。

## 对比入口

[地图与模型叠图对比](compare.html)：可切换原导览图投影 / 拉正俯视图、修改前 / 修改后，并分别控制值机、安检、商业与其他设施图层及透明度。主模型页面已增加入口。

两版均从实际 GLB 读取三角面，使用相同外围四锚点配准。未用手画轮廓冒充模型，也未单独移动新版来贴合地图。修订前文件及源布局保存在 archive/r4-layout。

## 本次修正

- 重新辨认 P01 图例：K/L 两侧长排设备对应绿色 Safety Check 标志。旧版将其误建为值机柜台，并错误地把安检集中在西端。
- 删除误建的两排值机柜台、旧西端安检与旧后侧隔墙。按图中两个范围重建两组安检，值机柜台保留在 K/L 岛朝向公共大厅的一面。
- 商业从西侧小块改为后侧连续的四段凹凸店铺轮廓和两端折线体量。凹多边形正确三角化，清除旧简化块及没有充分位置依据的西侧商业上夹层连接桥。
- 出发动线改为值机岛前 → 西侧安检 → 后侧商业通路 → 原窄缓坡连接通道。安检观察相机同步更新。
- 保留上一版主楼屋顶、廊桥、到达夹层及窄连接通道修正。

## 验证

实际导出网格切片检查通过：公共大厅至候机区可达；分别封闭另一组安检时，西、东两组都能独立通往候机区；同时封闭两组后无法绕行。全部 24 条到达/出发路线采样无阻挡，原到达分流、缓坡支撑和廊桥检查仍通过。

地图回投检查覆盖 8 个核心/商业轮廓；模型顶点对源描线误差小于 0.001 像素，仅证明模型忠实执行描线，**不是实物精度**。实际地图配准的四锚点残差为 15.752 像素（1809 × 1376 图幅）。叠图保留这种偏差。

网页核验覆盖模型视角、四层平面、动线切换、下载与移动端；对比页另检查两种投影、两版切换、透明度、分类和地图暂显。验证结果见 tests/*-validation.json，正式 HTTP 文件一致性见 tests/delivery-manifest.json。

## 仍未严格对应的内容

导览图为示意轴测图，其蓝色顶面轮廓不等于测绘底层墙线。外围透视形变与锚点残差仍会造成局部边界偏差，K/L 内部服务凹口、准确柜台/安检模块数量、店内隔间和上夹层范围未全部复原。本次采用的 12 / 10 个安检模块属于示意细分，不能认定为实际开放通道数。

导览图不能确定竖向剖面。楼层标高、屋顶曲率仍由已有实拍与推断建立，不能宣称严格符合测量。远机位插图位置仍未确定。
